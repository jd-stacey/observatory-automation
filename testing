if rotator_driver and hasattr(rotator_driver, 'set_tracking_target'):
    rotator_driver.set_tracking_target(
        target_info.ra_j2000_hours,
        target_info.dec_j2000_deg
    )
    logger.info("Field rotation target set")
